<h1>Listando Ações</h1>

<table>
  <tr>
    <th>Controller name</th>
    <th>Action name</th>
  </tr>

<% @acoes.each do |acao| %>
  <tr>
    <td><%=h acao.controller_name %></td>
    <td><%=h acao.action_name %></td>
    <td><%= link_to 'Mostrar', acao %></td>
    <td><%= link_to 'Editar', edit_acao_path(acao) %></td>
    <td><%= link_to 'Excluir', acao, :confirm => 'Você tem certeza?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova ação', new_acao_path %>
